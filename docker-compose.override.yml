version: "3.8"
services:
  api:
    build:
      context: ./api/
      dockerfile: ../docker/Dockerfile.api

    ports:
      - "2345:2345"

  uploader:
    build:
      context: ./uploader/
      dockerfile: ../docker/Dockerfile.uploader

networks:
  traefik:
    name: traefik
    attachable: true
